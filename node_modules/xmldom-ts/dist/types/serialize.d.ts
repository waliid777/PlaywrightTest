import './types';
import { VisibleNamespaces } from './types';
export declare function serializeToString(node: Node, buf: string[], isHTML?: boolean, nodeFilter?: NodeFilterTS, visibleNamespaces?: VisibleNamespaces): number | undefined;
