import './types';
import { MutableNode } from './types';
export declare function cloneNode<T extends MutableNode>(doc: Document, node: T, deep: boolean): T;
export declare function getTextContent(n: Node): string | null;
