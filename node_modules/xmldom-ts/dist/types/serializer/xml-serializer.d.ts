import '../types';
import { NodeFilterTS } from '../types';
export declare class XMLSerializerImpl implements XMLSerializer {
    serializeToString(node: Node, isHtml?: boolean, nodeFilter?: NodeFilterTS): string;
}
