import '../types';
import { NodeImpl } from '../node';
export declare class DummyDocumentFragment extends NodeImpl implements DocumentFragment {
    getElementById(_elementId: string): HTMLElement | null;
}
