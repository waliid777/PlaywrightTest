import './types';
import { DummyCharacterData } from './dummy/dummy-character-data';
export declare class CharacterDataImpl extends DummyCharacterData {
    _data: string;
    length: number;
    data: string;
    substringData(offset: number, count: number): string;
    appendData(text: string): void;
    insertData(offset: number, text: string): void;
    appendChild<T extends Node>(_newChild: T): T;
    deleteData(offset: number, count: number): void;
    replaceData(offset: number, count: number, text: string): void;
}
