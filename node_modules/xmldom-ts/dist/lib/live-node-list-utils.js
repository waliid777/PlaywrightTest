"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
require("./types");
const utils_1 = require("./utils");
function _updateLiveList(list) {
    const inc = utils_1.isDocument(list._node) ? list._node._inc : list._node.ownerDocument._inc;
    if (list._inc !== inc) {
        const ls = list._refresh(list._node);
        // console.log(ls.length)
        list._length = ls.length;
        utils_1.copy(ls, list); // fixme: I am not sure this will work with the current extension of Array
        list._inc = inc;
    }
}
exports._updateLiveList = _updateLiveList;
//# sourceMappingURL=live-node-list-utils.js.map